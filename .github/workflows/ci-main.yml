name: CI - Main

on:
  push:
    branches: [main]

permissions:
  contents: read

concurrency:
  group: web-main-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build-and-test:
    uses: ./.github/workflows/_build-and-test.yml
    with:
      solution_file: Maliev.Web.slnx
